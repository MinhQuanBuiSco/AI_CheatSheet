# This is a template - DO NOT commit actual secrets to git
# Use sealed-secrets, external-secrets, or vault in production
apiVersion: v1
kind: Secret
metadata:
  name: data-processing-secrets
  namespace: data-processing
type: Opaque
stringData:
  postgres_host: "postgres.data-processing.svc.cluster.local"
  postgres_password: "CHANGE_ME_IN_PRODUCTION"
  redis_url: "redis://redis.data-processing.svc.cluster.local:6379"
  aws_access_key_id: "CHANGE_ME"
  aws_secret_access_key: "CHANGE_ME"
  encryption_key: "CHANGE_ME"
